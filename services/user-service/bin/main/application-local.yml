# 本地开发环境
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartmemo
  data:
    redis:
      host: localhost
      port: 6379
      password: smartmemo_dev

logging:
  level:
    com.smartmemo.user: DEBUG
    org.springframework.security: DEBUG
